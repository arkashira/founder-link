[project]
name = "founder_link"
version = "0.1.0"
description = "Simple drag-and-drop MVP builder core logic"
authors = [{name = "Axentx"}]
requires-python = ">=3.9"
dependencies = []

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
